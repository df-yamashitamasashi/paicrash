# GEMINI — VSCode エージェント指示ファイル

目的: このプロジェクトで VSCode 内の AI エージェントが「antigravity」と同等のルールと品質で動作するための明確な運用ルールとチェックリストを定義します。

参照: `GEMINI.md` と `GEMINI.md.base` をベースにしています。プロジェクト固有の要件は `GEMINI.md` を優先してください。

**要約（必読）**
- 作業レベル: 本番想定。MVP/PoC/ダミーデータは作らない（明示的な要求がある場合を除く）。
- 目的: 安全で高品質、実用的なコード変更・提案を VSCode で実行すること。
- コミュニケーション: 簡潔な計画→実行→検証のサイクルを守る。

**エージェントの振る舞い（高レベル）**
- 常に「本番品質」を基準にする。可読性・拡張性・テスト性・セキュリティを優先。
- 変更は最小限に留め、既存スタイルと整合するようにする。
- 変更する前に短い計画（3〜6ステップ）を提示し、合意がなければ実行しない。
- 不確かな点は短く明確な質問を投げる（複雑な設計は一度に決めない）。

**ファイル編集ルール**
- すべてのソース編集は `apply_patch` フォーマットで行うこと（VSCode エージェントでは `apply_patch` を使用）。
- 1 パッチ＝1 意味のある変更。複数ファイル変更が必要な場合は、単一のパッチでまとめて行う。
- 不要なフォーマット変更はしない。既存コードのインデント・スタイルを維持する。
- 変更箇所には説明（`explanation`）を付ける。

**事前・事後のメッセージ習慣（ツール呼び出し）**
- ツール呼び出し前に 1 文の「何をするか」を報告する（例: “依存を更新してテストを実行します。”）。
- 複数の関連ツールを連続で実行する場合は一括で説明する。
- 主要なファイル変更（>3 ファイル）を行ったら進捗を報告する（短い箇条書き）。

**設計・実装ポリシー**
- ドキュメント・ファースト: 仕様が不足している場合は `docs/` の追加を提案し、小さな `requirements.md` を作る。
- テスト: 変更には可能な限り単体テストを追加。TypeScript 環境では `npx tsc --noEmit` を実行して型チェック。
- デバッグ: 変更を加えたらローカルで `npm install` → `npx tsc --noEmit` → `npm run dev`（または `pnpm dev`）の順で確認を推奨。

**セキュリティとライセンス**
- 秘密情報や鍵は絶対に出力・コミットしない。環境変数のみ参照する。
- 外部コードを追加する際は依存のライセンスと安全性（脆弱性）を確認する。

**コミットと PR の方針**
- コミットメッセージは要約 + 参照チケット（あれば）を含める（Conventional Commits 推奨）。
- PR では変更点の要点、動作確認手順、関連ドキュメントを必ず記載する。

**実行コマンドの例（VSCode ターミナルで実行）**
```bash
# 依存インストール (npm 環境)
npm install
# 型チェック
npx tsc --noEmit
# 開発サーバ起動
npm run dev
```

**VSCode 用の具体的なルール（使い方）**
- 変更提案: 変更前に「計画」を提示。計画承認後に `apply_patch` を実行。
- テスト: 型チェックと主要なユニットテストを実行してから変更を告知。
- 小さな改善（表記揺れ・コメント修正等）は即時実行してよいが、機能変更は常にユーザー確認を得る。

**チェックリスト（変更時）**
- [ ] 目的が `GEMINI.md` と齟齬無いか
- [ ] ドキュメントが更新されたか（必要なら `docs/` に追加）
- [ ] 型チェック（`npx tsc --noEmit`）がパスする
- [ ] 既存のスタイルと一致する（Prettier/ESLint）
- [ ] テストが追加/更新されている（可能なら CI でも確認）

---

このファイルは VSCode 内での AI 操作方針です。より厳密な個別ルールはプロジェクト内 `GEMINI.md` を参照してください。修正希望があれば指定してください。